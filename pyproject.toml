[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "awp-tf"
version = "1.0.0"
requires-python = ">=3.9"

dependencies = [
    "tensorflow",
]

[tool.setuptools.packages.find]
include = ["awp_tf*"]